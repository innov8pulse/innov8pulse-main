import React from "react";
import HomeComponent from "../components/HomeComponent"; // Default import

const Home = () => {
    return <HomeComponent />;
};

export default Home; // Ensure it's a default export
